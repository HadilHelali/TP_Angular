import { ArcdirDirective } from './arcdir.directive';

describe('ArcdirDirective', () => {
  it('should create an instance', () => {
    const directive = new ArcdirDirective();
    expect(directive).toBeTruthy();
  });
});
